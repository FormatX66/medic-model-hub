services:
  hub:
    build: .
    container_name: medic-model-hub
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:8090"
    env_file:
      - .env
    environment:
      PORT: 8090
    volumes:
      - ./data:/app/data  # QPU job ledger survives rebuilds
